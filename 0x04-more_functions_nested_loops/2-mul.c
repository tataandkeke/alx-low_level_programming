#include "main.h"
#include <stdio.h>

/**
 * mul - multiply 2 numbers
 * @a: input
 * @b: input 2
 * Return: return 1 if digit, return 0 otherwise
 */

int mul(int a, int b)
{
	int sum = a * b;

	return (sum);
}
