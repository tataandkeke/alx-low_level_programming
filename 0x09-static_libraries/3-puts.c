#include "main.h"
#include <stdio.h>
#include <string.h>


/**
 * _puts - reset the value a pointer is refereing too
 * @str: input pointer
 * Return:  return 0
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
