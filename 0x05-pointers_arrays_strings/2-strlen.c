#include "main.h"
#include <stdio.h>
#include <string.h>


/**
 * _strlen - returns length of a string
 * @s: input pointer
 * Return:  return 0
 */
int _strlen(char *s)
{
	int a;
	char b[] = *s;

	for (a = 0; b[] != '\0'; ++a)
	{

	}

	return (a);
}
