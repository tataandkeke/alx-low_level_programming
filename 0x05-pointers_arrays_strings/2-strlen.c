#include "main.h"
#include <stdio.h>
#include <string.h>


/**
 * _strlen - returns length of a string
 * @s: input pointer
 * Return:  return 0
 */
int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; ++a)
	{

	}

	return (a);
}
