#include "main.h"
#include <stdio.h>
#include <string.h>


/**
 * rev_string - print in reverse
 * @s: input pointer
 * Return:  return 0
 */

void rev_string(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{

	}

	for (a--; a >= 0; a--)
	{
		_putchar(s[a]);
	}

}
