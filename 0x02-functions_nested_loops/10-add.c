#include <unistd.h>
#include "main.h"

/**
 * add - add 2 integers togehter
 * descriptions : prints all the lower case character
 * @a: first integer
 * @b: second interger
 * Return: + when more than zero, 0 when zero, - when negative
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
