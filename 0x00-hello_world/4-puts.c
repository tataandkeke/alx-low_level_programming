#include <stdio.h>
#include <string.h>

/**
 * this is to put
 */

int main(void)
{
	char str1[55];

	strcpy(str1, "\"Programming is like building a multilingual puzzle");
	puts(str1);

	return (0);
}
